#!/usr/bin/env sh

# Example commands
# Start full stack    - docker compose --profile=* up
# Start reverse proxy - docker compose --profile=cache up
# Start database      - docker compose --profile=database up
# Start Solr search   - docker compose --profile=search up
